---
const faqItems = [
  {
    question: "Puis-je entrer et sortir librement du festival pendant la journée ?",
    answer: "Oui, avec votre billet, vous avez la liberté d'entrer et de sortir des lieux du festival autant de fois que vous le souhaitez pendant la journée."
  },
  {
    question: "Est-ce que les entrées enfants sont gratuites au festival ?",
    answer: "Oui, les enfants de moins de 12 ans bénéficient d'une entrée gratuite au festival."
  },
  {
    question: "Les animaux sont-ils autorisés ?",
    answer: "Non, pour des raisons de sécurité et d’hygiène, les animaux ne sont pas autorisés sur le site du festival."
  },
  {
    question: "Puis-je apporter ma propre nourriture et mes boissons ?",
    answer: "Non, il est interdit d’apporter de la nourriture et des boissons de l'extérieur. De nombreux stands de restauration seront disponibles sur place."
  }
];
---

<section class="bg-[#FAF7E7] p-6 rounded-lg shadow-md max-w-3xl mx-auto">
  <h2 class="text-3xl font-bold text-center text-[#5A4403] mb-6">FAQ</h2>

  <div x-data="{ openIndex: null }" class="space-y-4">
    {faqItems.map((item, index) => (
      <div class="border border-gray-300 rounded-lg overflow-hidden">
        <!-- Bouton question -->
        <button
          class="w-full text-left px-4 py-3 font-semibold flex justify-between items-center transition-all duration-200"
          :class="openIndex === index ? 'bg-yellow-500 text-black' : 'bg-[#D6E3D7] text-black'"
          @click="openIndex = openIndex === index ? null : index"
        >
          <span>{item.question}</span>
          <span x-text="openIndex === index ? '−' : '+'"></span>
        </button>

        <!-- Réponse (affichée seulement si openIndex === index) -->
        <div x-show="openIndex === index" x-transition class="p-4 bg-gray-100 border-t border-gray-300">
          {item.answer}
        </div>
      </div>
    ))}
  </div>
</section>

<!-- Inclure Alpine.js si ce n'est pas déjà fait -->
<script src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
